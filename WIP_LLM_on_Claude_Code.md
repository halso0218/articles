# Claude Codeにおける生成AIの構造理解

生成AIの構造の理解から、将来性と限界を把握し、活用できるようになることを期待する。

# 生成AI共通の特徴

1. **ステートレスであること**  
   1回の入力に対して1回の出力をするのみであり、AI単体では前の処理の内容をまったく覚えていない。  
   会話が成り立つのは、過去の会話の内容を次の入力の前に付け加えるという処理を別途行い、生成AIに渡しているからであり、モデルが前回の会話を覚えているわけではない。

2. **入力データには上限があること**  
   1回に入力できるデータサイズには上限がある。したがってこれが事実上の記憶の上限となる。  
   文章や画像は数値の列に変換され、その1つの数値が1トークンに相当する。画像は文字よりも大量のトークンを消費するため注意が必要。  
   AIによっては「これ以上会話できません」と表示されるのはそのためである。  
   ※ただし現在は100万トークン程度まで利用できるようになっている。

# Claude Codeがチャット系のAIと違うところ

1. **個性を出すために、入力の先頭にプロンプトを自動的に付け加える**  
   Claude CodeではCLAUDE.mdというファイルを起動時に読み込む仕組みになっている。例えば、「回答は必ず日本語で、敬語は使わずフランクに」「分からない場合は推測せず正直に不明と答える」「コードの説明より先に結論を述べる」といったペルソナや口調の設定を記述しておくことで、毎回指示しなくてもその個性に沿った動作をさせることができる。

2. **PC上の操作ができる**
   - ファイルの読み書きができる
   - 検索ができる

   これらによって、作成したプログラムを保存したり、サイトをクロールしてデータをまとめたり、記憶させたい内容をCLAUDE.mdに書き込ませたりすることができる。

3. **スキルという形で定型処理を定義できる**  
   スキルとは、特定の作業手順をMarkdownファイルとして記述しておき、Claude Codeに読み込ませる仕組みである。「このスキルを使って」と指示するだけで、ファイルに書かれた手順通りに処理を実行してくれるため、複雑な指示を毎回入力する手間を省くことができる。

4. **AIがAIを指示できる**  
   AIが複数の別のAIにタスクを依頼し、それを取りまとめることができる。

5. **入力が長くなったら圧縮される**  
   過去のやり取りを要約することで、継続的に会話を続けることができるようになっている。

# 以上を踏まえて

1. **まずは短めに**  
   入力データに上限があるため、冗長に書かないことが重要。AIがすでに知っていることをわざわざ書かない（例：DRY、SOLIDといった原則は現在のClaude Codeにとっては当然の前提となっている）。  
   CLAUDE.mdについてもAnthropicは200行以下を推奨しているが、実際にはより短い方が望ましい。

2. **Plan Modeを活用する**  
   誤った内容が含まれていたり、対話が長くなることで以前に伝えたルールが欠落したりすることがある。  
   そういった場合は、新たに必要な部分だけを入力として伝える。Plan Modeは実行せずに計画のみを立てるためのモードであるが、不要な情報を省いた簡潔な入力を構築するためにも活用できる。

3. **役割を分ける**  
   役割ごとに伝える情報を絞る。例えば、製造担当AIには詳細設計のみを、セキュリティ監査AIには検査項目のみを渡すというように、不要な情報を削ることで、伝えた内容を漏れなく対応してもらいやすくなる。

# (おまけ)LLMの将来

1. **なんでもできるAIから得意分野のあるAIへ**  
   人間でもビジネス用語には詳しくても最近のアイドル事情にはまったく無知な人がいるように、生成AIもそれぞれ得意分野を持つ専門AIに聞けばよいという時代がすでに来つつある。  
   Claude Codeはアニメやゲームの知識は乏しいが、コードに関しては非常に優秀といった具合だ。

2. **記憶できるAIへ**  
   モデルのパラメータは固定されているため、基本的に同じ入力に対して同じ出力をする（変化するのは、そういった仕組みを外部から組み込んでいる場合に限られる）。  
   真の意味での記憶とは、モデル自体を学習させパラメータを更新することにある。それによって、同じ入力でも学習結果を踏まえた回答が可能になる。

   現在それが難しいのはコストの問題によるところが大きい。1つのモデルの学習に数百億円規模の費用がかかるため、現状では同じモデルを多くのユーザーで共有する形が一般的となっている。

   一人に一つ以上のモデルを持てて、しかも容易に追加学習できるようになれば、人を雇うような感覚でAIを活用できる時代が来るだろう。

   私はBonsai-8Bというモデルにその可能性の芽を見たが、いかがだろうか。